import React from 'react';

// ! This is a mock page. We will probably create it in the nearest future
const NotFound: React.FC = () => (
  <div>
    <h1>PAGE NOT FOUND</h1>
  </div>
);

export default NotFound;
